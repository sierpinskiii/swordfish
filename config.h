// config.h
#include <string>

/* reader mode ("json", "pipe") */
static const std::string MODE = "json";

/* named pipe locations */
static const std::string LINK_LOC = "test.json";

/* terminal character limit */
const int TEMP_SIZE = 2000;

// const int pipe_n = sizeof(pipes) / sizeof(pipes[0]);
